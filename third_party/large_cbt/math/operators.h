#pragma once

#include <cmath>
#include <cstdint>
#include <cstring>

using std::log2;
using std::memset;

std::uint32_t countbits(std::uint32_t value);
std::uint32_t countbits(std::uint64_t value);

